# 进度快照 2 - 依赖与备份完成

## 已完成内容
- 已读取最新快照：`1-项目克隆完成.md`。
- 已读取服务器文档：`docs/远程服务器.md`，确认包含两组服务器地址和 key。
- 已初始化缺失子模块：`libs/hbb_common`，当前检出到 `.gitmodules` 指定提交 `a920d00945e1d2441b3f77b2677054cb8c3d9dd2`。
- 已创建本功能阶段备份目录：`backup/1-多服务器中继功能阶段`。

## 当前修改到哪个模块
- 当前阶段：模块 1 - 依赖与备份。
- 本模块未修改 RustDesk 业务源码，仅补齐依赖子模块并创建阶段备份。

## 具体修改的代码前后对比
本模块没有业务源码级改动。

```text
修改前：
libs/hbb_common 子模块目录为空，无法编译依赖 hbb_common 的 Rust 代码。
backup 目录不存在，尚未创建本功能阶段备份。

修改后：
libs/hbb_common 已初始化到 a920d00945e1d2441b3f77b2677054cb8c3d9dd2。
新增 backup/1-多服务器中继功能阶段，备份了本阶段预计修改的关键源码文件。
```

## 待办清单（未完成）
- 实现多服务器 profile 配置模型。
- 从 `docs/远程服务器.md` 解析两组服务器配置并导入。
- 增加 profile 延迟测试与保存逻辑。
- 接入后端按服务器选择 key / relay。
- 改造 Flutter 设置页卡片 UI。
- 运行测试、构建检查并清理临时缓存。

## 下一步要做什么
- 开始模块 2：新增 `server-profiles` 配置模型、旧单服务器配置迁移、文档导入和基础连通性测试接口。
