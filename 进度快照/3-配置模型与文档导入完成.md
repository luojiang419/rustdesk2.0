# 进度快照 3 - 配置模型与文档导入完成

## 已完成内容
- 新增 `src/server_profiles.rs`：
  - 定义 `server-profiles` 多服务器配置模型。
  - 支持旧单服务器配置迁移为第一张 profile 卡片。
  - 支持保存 profiles 时同步旧配置项，保持兼容。
  - 支持从 `docs/远程服务器.md` 解析服务器地址和 key。
  - 支持批量测试 profile 的 ID Server TCP 延迟。
- 在 `src/lib.rs` 注册 `server_profiles` 模块。
- 在 `src/flutter_ffi.rs` 增加 Flutter 可调用接口：
  - `main_get_server_profiles`
  - `main_save_server_profiles`
  - `main_import_server_profiles_from_doc`
  - `main_test_server_profiles`

## 当前修改到哪个模块
- 当前阶段：模块 2 - 配置模型与文档导入。
- 下一阶段将进入模块 3：后端连接路径按 profile 选择 key / relay。

## 具体修改的代码前后对比
### 1. 模块注册

```diff
修改前：
mod custom_server;
mod lang;

修改后：
mod custom_server;
mod lang;
pub mod server_profiles;
```

### 2. FFI 接口

```diff
修改前：
pub fn main_test_if_valid_server(server: String, test_with_proxy: bool) -> String {
    test_if_valid_server(server, test_with_proxy)
}

修改后：
pub fn main_test_if_valid_server(server: String, test_with_proxy: bool) -> String {
    test_if_valid_server(server, test_with_proxy)
}

pub fn main_get_server_profiles() -> String {
    crate::server_profiles::get_profiles_json()
}

pub fn main_save_server_profiles(json: String) -> String {
    crate::server_profiles::save_profiles_json(&json)
}

pub fn main_import_server_profiles_from_doc(path: String) -> String {
    crate::server_profiles::import_profiles_from_doc(&path)
}

pub fn main_test_server_profiles(json: String) -> String {
    crate::server_profiles::test_profiles_latency_json(json)
}
```

### 3. 新增配置模型核心结构

```rust
pub const OPTION_SERVER_PROFILES: &str = "server-profiles";
pub const OPTION_AUTO_SERVER_SWITCH_THRESHOLD_MS: &str =
    "auto-server-switch-threshold-ms";
pub const DEFAULT_AUTO_SERVER_SWITCH_THRESHOLD_MS: u64 = 300;

#[derive(Debug, Clone, Default, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "camelCase")]
pub struct ServerProfile {
    pub id: String,
    pub name: String,
    pub id_server: String,
    pub relay_server: String,
    pub api_server: String,
    pub key: String,
    #[serde(default = "default_enabled")]
    pub enabled: bool,
}
```

## 验证结果
- 已通过：
  - `rustfmt --edition 2021 --check src\server_profiles.rs`
  - `rustfmt --edition 2021 --check src\flutter_ffi.rs`
- 未完成：
  - `cargo test --lib server_profiles --no-default-features` 被本机环境阻塞。
  - 阻塞原因：`magnum-opus` 和 `scrap` 的 build script 提示缺少 `VCPKG_ROOT`。

## 待办清单（未完成）
- 修改 `hbb_common::Config` 的多服务器选择逻辑。
- 修改后端连接、relay、health check、端口转发路径，按 profile 选择 key。
- Flutter 侧生成桥接并接入卡片 UI。
- 模块 5 处理 `VCPKG_ROOT` / vcpkg 环境后重新运行 Rust 测试。

## 下一步要做什么
- 开始模块 3：接入多服务器后端连接逻辑，重点解决两个服务器 key 不同导致的握手问题。
