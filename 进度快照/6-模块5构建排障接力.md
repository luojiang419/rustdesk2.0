# 6-模块5构建排障接力

## 已完成内容
- 延续模块 5「测试、构建、清理」继续推进，已完成 Flutter 3.38.8 兼容补丁。
- `flutter/lib/common.dart` 中适配新版 Material 类型：
  - `DialogTheme(...)` 改为 `DialogThemeData(...)`
  - `TabBarTheme(...)` 改为 `TabBarThemeData(...)`
- `flutter/pubspec.yaml` 中将 `extended_text` 从 `14.0.0` 升级到 `15.0.2`，并已重新执行 `pub get`，`flutter/pubspec.lock` 已同步更新。
- Flutter 侧测试已恢复通过：
  - `D:\flutter\bin\flutter.bat test test\server_settings_dialog_test.dart`
  - 结果：`2` 个测试全部通过。
- 已完成 Flutter Rust Bridge 生成环境补齐：
  - 安装 LLVM 22.1.7
  - 安装 `flutter_rust_bridge_codegen 1.80.1`
  - 成功生成本地桥接文件：
    - `flutter/lib/generated_bridge.dart`
    - `src/bridge_generated.rs`
    - `flutter/macos/Runner/bridge_generated.h`
- 已完成 Rust Windows 构建依赖补齐：
  - 安装 `7zip`
  - 安装并引导最新 `vcpkg`
  - 已成功安装最小静态依赖：
    - `libvpx:x64-windows-static`
    - `libyuv:x64-windows-static`
    - `opus:x64-windows-static`
    - `aom:x64-windows-static`

## 当前修改到哪个模块
- 当前所在模块：模块 5「测试、构建、清理」。
- 当前状态：Flutter 测试已通过，Rust 构建链仍有阻塞，尚未完成最终构建产物与缓存清理。

## 具体修改的代码前后对比

### 1. Flutter 3.38 主题类型兼容
修改前：
```dart
dialogTheme: DialogTheme(
  backgroundColor: MyTheme.canvasColor,
),
tabBarTheme: const TabBarTheme(
  dividerColor: Colors.transparent,
),
```

修改后：
```dart
dialogTheme: DialogThemeData(
  backgroundColor: MyTheme.canvasColor,
),
tabBarTheme: const TabBarThemeData(
  dividerColor: Colors.transparent,
),
```

### 2. `extended_text` 依赖升级
修改前：
```yaml
extended_text: 14.0.0
```

修改后：
```yaml
extended_text: 15.0.2
```

## 当前阻塞点
- 运行 Rust 校验时，问题已经从 `VCPKG_ROOT` 缺失推进到了 `libs/scrap` 的 `bindgen` 生成结果异常。
- 当前失败命令：
  - `cargo test --lib server_profiles --no-default-features`
  - `cargo check --features flutter`
- 共同阻塞点：
  - `libs/scrap` 生成的 `vpx_ffi.rs` / `aom_ffi.rs` 中，多个 codec 配置结构体被生成为不透明结构体，仅包含：
```rust
pub struct vpx_codec_enc_cfg {
    pub _address: u8,
}
```
- 导致后续代码访问字段时报错，例如：
```rust
cfg.g_w = width as _;
cfg.g_h = height as _;
```
- 已确认 `vcpkg` 安装目录中的原始头文件本身是完整结构体定义，不是依赖缺头：
  - `C:\Users\jiang\vcpkg\installed\x64-windows-static\include\vpx\vpx_encoder.h`
  - `C:\Users\jiang\vcpkg\installed\x64-windows-static\include\aom\aom_encoder.h`
- 因此当前更可能是：
  1. `bindgen 0.65` 与本机 `libclang 22` 组合兼容性问题；
  2. `libs/scrap/build.rs` 的绑定生成参数对当前环境不够稳。

## 已验证结果
- `D:\flutter\bin\flutter.bat pub get`：通过。
- `D:\flutter\bin\flutter.bat test test\server_settings_dialog_test.dart`：通过。
- `git diff --check`：通过，仅存在换行符提示，无空白错误。
- `setx VCPKG_ROOT "C:\Users\jiang\vcpkg"`：已持久化。
- `vcpkg` 最小静态依赖安装：通过。

## 待办清单（未完成）
- 排查并修复 `libs/scrap` 的 `bindgen` 生成异常。
- 重新运行：
  - `cargo test --lib server_profiles --no-default-features`
  - `cargo check --features flutter`
- 视结果补跑必要构建。
- 生成模块 5 最终快照。
- 清理临时缓存，避免污染项目目录。

## 下一步
1. 优先检查 `libs/scrap/build.rs` 与 `Cargo.lock` 中 `bindgen` 版本，判断是升级 `bindgen`、切换 `libclang`，还是改为更稳定的绑定生成策略。
2. 在最小改动原则下修复 Rust 构建链。
3. 完成 Rust 校验后，继续模块 5 的最终构建、清理和验收。
